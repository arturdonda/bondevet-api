import { inviteEmailTemplate } from './invite';
import { resetPasswordEmailTemplate } from './reset-password';

export const EMAIL_TEMPLATES = {
	invite: inviteEmailTemplate,
	resetPassword: resetPasswordEmailTemplate,
};
